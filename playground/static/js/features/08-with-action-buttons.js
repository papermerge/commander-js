window.addEventListener('DOMContentLoaded', () => {
    let DC = DocumentCommander,
        commander1;

    DC.urlconf.prefix = '/08-with-action-button';

    commander1 = new DC.CommanderView({'el': "#commander1"});
});